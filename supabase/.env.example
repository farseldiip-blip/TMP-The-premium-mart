# Supabase local env — copy to supabase/.env for CLI if needed (gitignored).
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=[JWT_2]
SUPABASE_SERVICE_ROLE_KEY=[JWT_1]
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres
