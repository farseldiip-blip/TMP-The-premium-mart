{
  "name": "tpm-the-premium-mart",
  "version": "1.0.0",
  "description": "TPM — The Premium Mart. Static, mobile-first mart website: signature drinks, coffee, bites and curated finds. No framework, no build step.",
  "type": "module",
  "scripts": {
    "dev": "python -m http.server 5173 --bind 127.0.0.1",
    "preview": "npx serve . -l 5173",
    "serve": "python -m http.server 5173 --bind 127.0.0.1"
  },
  "devDependencies": {
    "gsap": "^3.15.0",
    "puppeteer-core": "^25.9.0"
  }
}
