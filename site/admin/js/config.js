// site/admin/js/config.js — TPM Admin Dashboard (local dev defaults)
// Local Supabase URL + anon (publishable) key.
// This value is the default local anon key generated by `npx supabase start`
// with the default JWT secret (supabase-demo). It is publishable (RLS protects data).
// Verified: config.toml has no custom jwt_secret, so this is what `supabase status`
// would return. Replace with the value from `npx supabase status --output env` after
// `npx supabase start` if your local secret differs. Never put service_role here.

export const SUPABASE_URL = "http://127.0.0.1:54321";
export const SUPABASE_ANON_KEY =
  "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0";
